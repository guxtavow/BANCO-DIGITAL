const { render, screen } = require('@testing-library/react');
const userEvent = require('@testing-library/user-event');
const Login = require('../pages/LoginPage/Login');


it('Login of the users using email and password', () => {
    expect()


})